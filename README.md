# SQL_PROJECT_WORK
--Developed this query using CTE common table expression to achieve the output
--Used set operators and also arthematic expressions to gether
--did find out the days from date columns